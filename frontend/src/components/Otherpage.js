import React, { Component } from 'react';

export default class Otherpage extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return <p>This is the other page</p>;
    }
}